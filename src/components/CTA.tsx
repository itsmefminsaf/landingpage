const CTA = () => {
  return (
    <button className="bg-blue-50 rounded-3xl text-black px-5 py-2 hover:scale-105 hover:font-extrabold duration-300">
      Book now!
    </button>
  );
};

export default CTA;
